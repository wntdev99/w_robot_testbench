# 의존성 SSOT = package.xml (rosdep). 빌드 = colcon ament_python (setup.py).
# 본 파일은 ruff 린트 설정만 (개발 편의). [project]/[build-system]은 두지 않음
# — ament_python(setup.py) 빌드와 충돌 방지.
[tool.ruff]
line-length = 100
target-version = "py312"
